---
const temp = Astro.url.searchParams.get("temp") || "notemp";
const pres = Astro.url.searchParams.get("pres") || "nopres";
console.log(temp, pres);

import { supabase } from "../lib/supabase";

if (temp) {
	const { error } = await supabase
		.from("measurements")
		.insert({ temperatur: parseFloat(temp), tryk: parseFloat(pres) });
	if (error) console.log(`Insert error: ${error}`);
}
---

<html lang="en">
	<head>
		<link href="/styles.css" type="text/css" rel="stylesheet">
		<title>
			Receiver Page
		</title>
	</head>
	<body>
		<button class="return" type="button" onclick="location.href='../'">Tilbage til forside</button>
		<p>
			Denne side er til at modtage data, og vil derfor ikke vise noget.
		</p>
	</body>
</html>